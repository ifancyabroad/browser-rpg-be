export enum Status {
	Alive = "alive",
	Dead = "dead",
	Retired = "retired",
}

export enum State {
	Idle = "idle",
	Battle = "battle",
}

export enum BattleState {
	Active = "active",
	Complete = "complete",
}
