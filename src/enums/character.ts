export enum CharacterClass {
	Warrior = "warrior",
	Rogue = "rogue",
	Mage = "mage",
}

export enum Status {
	Alive = "alive",
	Dead = "dead",
	Retired = "retired",
}

export enum State {
	Idle = "idle",
	Battle = "battle",
}
